# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v57.0.0/ before writing any code.

# Rules
- Do not style the application yet, focus on getting the functionality correct then the UI/UX designs will come through later
- You must use nativewind to style components
- If dependency installs are required, ask the user, otherwise if buildable then build it and include tests
- When creating UI, try not to use Subheadings if the Header is already descriptive enough. The header should contain the bulk of the information
- Do not start the application after your changes
